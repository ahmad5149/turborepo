const mainPageQuery = `*[_type == "main"][0]`;
const homePageQuery = `*[_type == "home"][0]`;
const aboutPageQuery = `*[_type == "about"][0]`;
const howItWorksQuery = `*[_type == "howItWorks"][0]`;
const faqsQuery = `*[_type == "faqs"][0]`;
const sellTradeQuery = `*[_type == "sellOrTrade"][0]`;
const supportPageQuery = `*[_type == "supportPage"][0]`;
const careerQuery = `*[_type == "careerPage"][0]`;
const defaultPageQuery = `*[_type == "defaultPage"][0]`;
const popOutQuery = `*[_type == "popOut"][0]`;
const carDetailsQuery = `*[_type == "carDetails"][0]`;
const flyoutQuery = `*[_type == "flyoutFinancing"][0]`;
const findYourRideQuery = `*[_type == "findYourRide"][0]`;
const inventoryQuery = `*[_type == "inventory"][0]`;
const offsiteFindYourRideQuery = `*[_type == "offsiteFindYourRide"][0]`;
const offsiteInvQuery = `*[_type == "offsite"][0]`;
const offsiteCarDetailsQuery = `*[_type == "offsiteCarDetails"][0]`;

export {
    aboutPageQuery,
    homePageQuery,
    mainPageQuery,
    howItWorksQuery,
    faqsQuery,
    sellTradeQuery,
    supportPageQuery,
    careerQuery,
    defaultPageQuery,
    popOutQuery,
    carDetailsQuery,
    flyoutQuery,
    findYourRideQuery,
    inventoryQuery,
    offsiteFindYourRideQuery,
    offsiteInvQuery,
    offsiteCarDetailsQuery
};
